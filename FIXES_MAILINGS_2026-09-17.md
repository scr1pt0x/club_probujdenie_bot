# Рассылки: поддержка расширенного оформления Telegram

Статус: опубликовано в `main` и установлено на сервер 17.09.2026 с разрешения
владельца. Исправление: `b80566f`, ветка `codex/rich-message-mailings`.
Базовая версия: `d83ef03`.

## Подтверждённая причина

Read-only проверка сервера 17.09.2026: HEAD `d83ef03`, сервис active.
В журнале `club_probujdenie_bot.service` зарегистрированы четыре ввода:

- 07:04:20 UTC — `RICH_MESSAGE`, аудитория `all`;
- 07:05:13 и 07:05:46 UTC — `RICH_MESSAGE`, аудитория `former`;
- 07:06:56 UTC — `RICH_MESSAGE`, аудитория `all`.

Во всех случаях `text_length=0`, `caption_length=0`. Это не пустые сообщения:
их содержимое находится в поле `rich_message`. Обработчик разрешал только
`text`, `photo`, `video`, `document` и отклонял этот формат до предпросмотра.
Тест с синтетическим rich-сообщением воспроизвёл дефект до правки.

У прямых кнопок и выбора через «Свободную рассылку» одинаковые callback_data
и обработчик. Поэтому причина этих отказов — формат входящего сообщения,
а не отдельный маршрут кнопки. Это не доказывает причину всех прошлых жалоб.
Устаревшие кнопки `mailings:run:*` найдены в истории Git, но их использование
в проверенном журнале не подтверждено; их поведение этим исправлением не меняется.

## Исправление

- Общий обработчик принимает `rich_message` для всех четырёх аудиторий.
- Сообщение копируется целиком через `copyMessage`, без преобразования
  блоков в обычный текст и потери оформления.
- Подсказка перечисляет расширенный текст, фото, видео и документы.
- Предпросмотр, подтверждение, выбор получателей и защита от повторного
  запуска сохранены. Никакие сообщения автоматически не переотправлялись.

Основание по API: [RichMessage](https://core.telegram.org/bots/api#richmessage)
и [copyMessage](https://core.telegram.org/bots/api#copymessage).

## Проверки

- **143 passed**, включая изолированную локальную PostgreSQL.
- Добавлены 17 тестовых случаев: 16 сочетаний четырёх аудиторий, двух путей
  входа и двух форматов сообщения; отдельная регрессия rich-предпросмотра.
- Полная цепочка Dispatcher дополнительно проверена с `rich_message`.
- Проверены сброс старого предпросмотра, сохранение выбранной аудитории,
  отсутствие отправки до подтверждения и отсутствие повтора по той же кнопке.
- Ruff, проверка форматирования, `compileall`, `pip check`, `git diff --check`
  прошли. Производственная БД не изменялась.

Реальная доставка через Telegram в этой проверке не выполнялась: сетевой
транспорт в тестах подменён. После разрешённого выпуска нужен контрольный
предпросмотр у администратора; массовую рассылку подтверждает сам администратор.
Платежи, отсрочки, правила доступа и исключения участниц не изменены.

## Выпуск и проверка сервера

- Перед обновлением сохранены база, приложение и конфигурация в
  `/var/backups/club_probujdenie/20260917-rich-mailings.CwoUV4/`.
  Дамп проверен через `pg_restore --list`; каталог имеет права 700, файлы 600.
- Новый код проверен в отдельном каталоге на серверном Python с тестовыми
  реквизитами: **73 passed, 70 skipped**. Проверки PostgreSQL на рабочей базе
  не запускались. Тестовый pytest установлен вне рабочего виртуального окружения.
  Первая попытка проверки остановилась при сборе тестов из-за отсутствия
  переменных окружения в изолированном каталоге; повторный запуск с фиктивными
  реквизитами прошёл. Код приложения при этом не менялся.
- До перезапуска незавершённых кампаний рассылки в журнале было 0.
- Сервис перезапущен в 18:28:07 UTC; `active`, `NRestarts=0`, polling запущен.
  Локальный и внешний HTTPS `/api/healthz` отвечают `{"status":"ok"}`;
  в проверенном журнале нового запуска нет ошибок.
- До и после обновления: 532 пользователя, 8 льготниц, 0 ручных ограничений.
  Контрольная сумма флагов `access_exempt`/`access_suspended` совпала.
- `.env`, схема БД и отдельные серверные скрипты не изменялись.
- Реальные рассылки и контрольные сообщения участницам не отправлялись.
  Живой предпросмотр расширенного сообщения у администратора остаётся
  пользовательской проверкой; массовую отправку подтверждает администратор.
